   /*string someString;
   while (getline(fileIn, someString)) {
     fileIn >> someString;
   }
   // Use the stream in this function, for example read a string.
   //fileIn >> someString;
   return someString;*/
